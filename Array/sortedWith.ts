/* Autogenerated by /.tools/auto.ts */

import key from "../symbols/sortedWith.ts";
import value from "./methods/sortedWith.ts";

declare global {
  interface Array<T> {
    [key]: typeof value;
  }
  interface ReadonlyArray<T> {
    [key]: typeof value;
  }
}

Object.defineProperty(Array.prototype, key, { value });

export default key;
